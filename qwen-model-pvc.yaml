---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-qwen-model
  namespace: multimodal-demo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: gp3-csi

